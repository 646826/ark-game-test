{
  "name": "clockwork-conservatory",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Premium responsive HTML5 puzzle designed for Arkadium.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs dist",
    "preview": "node scripts/serve.mjs dist",
    "check": "node scripts/check.mjs",
    "test": "node scripts/test.mjs",
    "smoke": "python3 -u scripts/smoke_browser.py",
    "ci": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
